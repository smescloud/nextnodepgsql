apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ai-demo-tls
spec:
  secretName: ai-demo-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-production
  dnsNames:
    - nextnodepostgres-eu001.smescloud.com